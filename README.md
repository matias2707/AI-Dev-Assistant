# AI Dev Assistant - FastAPI example

Estructura mínima de ejemplo con FastAPI.

Instrucciones rápidas:

Instalar dependencias:

```bash
pip install -r requirements.txt
```

Ejecutar la aplicación:

```bash
uvicorn app.main:app --reload
```

Ejecutar tests:

```bash
pytest -q
```
